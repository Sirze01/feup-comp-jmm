import ioPlus;

class HelloWorld extends HelloClass{
    int a;
    int[] b;
    boolean c;
    String d;
    Type e;

	public static void main(String[] args) {
	    int a;
	    int[] b;
        boolean c;
        String d;
        Type e;

        a = 2;

        b[2] = 4;

	}

	public int sayHello(String args) {
	    int a;
        ioPlus.printHelloWorld();
        return args;
    }

    public HelloWorld sayHello(Int nr, String args) {
        int a;
        a = 0;
        while( a < 5 ){
            a = a + 1;
            ioPlus.printHelloWorld();
        }
        return this;
    }

    public int[] sayHello(Int nr, String args, String hello) {
            String b;
            int a;
            HelloWorld h;

            a = 3;

            if (a < 2){
                b[0] = 2;
            }
            else{
                b[0 + 2] = 3 + 2;
            }

            c = false;

            h.sayHello(1, b, b).sayHello(1,b,b).sayHello(1,b,b);
            return c;
        }
}